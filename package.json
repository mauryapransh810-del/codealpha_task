{
  "name": "social-media-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --port=3000 --host=127.0.0.1 --open=./index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
